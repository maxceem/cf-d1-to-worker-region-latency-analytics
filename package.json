{
  "name": "cf-worker-d1-regions-latency-stats",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Benchmark Cloudflare Worker targeted placements against a D1 database.",
  "author": "Maksym Mykhailenko (https://github.com/maxceem)",
  "license": "MIT",
  "scripts": {
    "benchmark": "node ./src/d1-placement-benchmark.mjs",
    "cleanup": "node ./src/resource-tracker.mjs",
    "report:html": "node ./src/build-html-report.mjs",
    "check": "node --check ./src/d1-placement-benchmark.mjs && node --check ./src/benchmark-worker.mjs && node --check ./src/resource-tracker.mjs && node --check ./src/build-html-report.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
