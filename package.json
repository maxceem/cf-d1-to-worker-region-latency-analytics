{
  "name": "cf-d1-to-worker-region-latency-analytics",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Benchmark Cloudflare Worker targeted placements against a D1 database.",
  "author": "Maksym Mykhailenko",
  "license": "MIT",
  "scripts": {
    "benchmark": "node ./src/run-benchmark.mjs",
    "cleanup": "node ./src/clean-resources.mjs",
    "site": "node ./src/build-html-site.mjs",
    "build:site": "node ./src/build-html-site.mjs --no-open",
    "check": "node --check ./src/run-benchmark.mjs && node --check ./src/benchmark-worker-source.mjs && node --check ./src/clean-resources.mjs && node --check ./src/build-html-site.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
